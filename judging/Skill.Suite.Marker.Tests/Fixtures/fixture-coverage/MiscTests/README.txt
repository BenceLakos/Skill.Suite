A fixture directory with no Cobertura report in it: its per-class run timed out or crashed.

The marker must emit no coverage event for MiscTests at all. A zero here would read as "this class covered
nothing", which is a finding about the submission; the truth is that nothing was measured.
